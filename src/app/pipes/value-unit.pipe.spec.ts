import { ValueUnitPipe } from './value-unit.pipe';

describe('ValueUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
